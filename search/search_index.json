{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Envist","text":"<p>Envist turns plain <code>.env</code> files into an intelligent, type-safe configuration layer for Python applications. Parse annotated environment variables, expand references, validate values, and log everything without adding external dependencies.</p> <p>TL;DR: Annotate your <code>.env</code> once, enjoy reliable configuration everywhere.</p>"},{"location":"#why-teams-choose-envist","title":"Why Teams Choose Envist","text":"<ul> <li>Built-in type casting with nested data structures such as <code>list&lt;int&gt;</code> or <code>dict&lt;str, list&lt;bool&gt;&gt;</code></li> <li>Variable expansion that understands chained references and prevents circular loops</li> <li>Validator decorator for enforcing business rules before your app starts</li> <li>Observable by design through the dedicated <code>EnvistLogger</code></li> <li>Zero dependencies and full backwards compatibility with traditional <code>.env</code> files</li> </ul>"},{"location":"#at-a-glance","title":"At a Glance","text":"<pre><code>APP_NAME &lt;str&gt; = Envist Demo\nPORT &lt;int&gt; = 8080\nDEBUG &lt;bool&gt; = true\nHOSTS &lt;list&gt; = localhost, 127.0.0.1\nDATABASE_URL &lt;str&gt; = postgresql://user:pass@${HOSTS[0]}:5432/app\nFEATURE_FLAGS &lt;dict&lt;str, bool&gt;&gt; = auth:true, caching:false\n</code></pre> <pre><code>from envist import Envist\n\nenv = Envist()\nprint(env.get(\"PORT\"))           # 8080 (int)\nprint(env.get(\"FEATURE_FLAGS\"))  # {'auth': True, 'caching': False}\n</code></pre>"},{"location":"#explore-the-documentation","title":"Explore the Documentation","text":"<ul> <li>New here? Start with the Getting Started guide.</li> <li>Need syntax examples? Check the Smart .env Syntax page.</li> <li>Looking for programmatic details? Dive into the API Reference.</li> <li>Browse ready-to-run snippets on the Examples &amp; Recipes page.</li> </ul>"},{"location":"#proven-in-real-projects","title":"Proven in Real Projects","text":"<p>Envist powers anything from local prototypes to production services. With a single import you get:</p> <ul> <li>Deterministic configuration loading for CI/CD pipelines</li> <li>Consistent typing for data science notebooks and ML jobs</li> <li>Declarative validation for platform engineering teams</li> </ul> <p>Ready to adopt? Install the package with <code>pip install envist</code> and keep reading. We'll handle the rest.</p>"},{"location":"examples/","title":"Examples &amp; Recipes","text":"<p>Kick-start your integration with copy-paste-friendly snippets drawn from real-world Envist usage.</p>"},{"location":"examples/#fastapi-application","title":"FastAPI Application","text":"<pre><code>from fastapi import FastAPI\nfrom envist import Envist\n\nenv = Envist()\n\napp = FastAPI(title=env.get(\"APP_NAME\", default=\"Envist API\"))\n\n@app.get(\"/health\")\ndef health():\n    return {\n        \"status\": \"ok\",\n        \"version\": env.get(\"APP_VERSION\", default=\"dev\"),\n        \"debug\": env.get(\"DEBUG\", default=False),\n    }\n</code></pre> <p>Pair this with a <code>.env</code> file:</p> <pre><code>APP_NAME &lt;str&gt; = Envist Service\nAPP_VERSION &lt;str&gt; = 1.2.0\nDEBUG &lt;bool&gt; = false\n</code></pre>"},{"location":"examples/#django-settings-bridge","title":"Django Settings Bridge","text":"<pre><code># settings.py\nfrom envist import Envist\n\nenv = Envist()\n\nSECRET_KEY = env.get(\"SECRET_KEY\")\nDEBUG = env.get(\"DEBUG\", default=False)\nALLOWED_HOSTS = env.get(\"ALLOWED_HOSTS\", default=[\"localhost\"])\nDATABASES = {\n    \"default\": env.get(\"DATABASE_CONFIG\", cast=\"json\"),\n}\n</code></pre>"},{"location":"examples/#data-pipelines","title":"Data Pipelines","text":"<pre><code>from envist import Envist\n\nenv = Envist(path=\"config/pipeline.env\")\n\nBATCH_SIZE = env.get(\"BATCH_SIZE\", default=1000)\nSOURCES = env.get(\"SOURCES\", cast=\"list&lt;str&gt;\")\nRETRY_POLICY = env.get(\"RETRY_POLICY\", cast=\"dict&lt;str, int&gt;\")\n</code></pre> <p><code>pipeline.env</code> may look like:</p> <pre><code>BATCH_SIZE &lt;int&gt; = 500\nSOURCES &lt;list&gt; = s3://bucket/raw, s3://bucket/enriched\nRETRY_POLICY &lt;dict&lt;str, int&gt;&gt; = attempts:3, backoff:5\n</code></pre>"},{"location":"examples/#cli-tools","title":"CLI Tools","text":"<pre><code>import click\nfrom envist import Envist\n\nenv = Envist()\n\n@click.command()\n@click.argument(\"key\")\n@click.option(\"--value\")\ndef set_env(key, value):\n    env.set(key, value)\n    env.save(pretty=True, sort_keys=True)\n    click.echo(f\"Updated {key}\")\n</code></pre>"},{"location":"examples/#advanced-logging-setup","title":"Advanced Logging Setup","text":"<pre><code>from envist.logger import configure_logger, create_stream_handler, create_json_handler\n\nconfigure_logger(\n    handlers=[\n        create_stream_handler(level=\"INFO\"),\n        create_json_handler(\"logs/envist.json\", level=\"DEBUG\"),\n    ],\n    level=\"DEBUG\",\n)\n</code></pre>"},{"location":"examples/#browse-the-repository","title":"Browse the Repository","text":"<p>More end-to-end scripts live in the <code>examples/</code> directory:</p> <ul> <li><code>examples/getting_started/01_basic_usage.py</code></li> <li><code>examples/advanced/01_custom_logging.py</code></li> <li><code>examples/advanced/04_validation.py</code></li> </ul> <p>Each script mirrors the concepts documented here, so you can experiment locally or adapt them to your stack.</p>"},{"location":"faq/","title":"FAQ","text":"<p>Find quick answers to the most common Envist questions.</p>"},{"location":"faq/#does-envist-work-with-plain-env-files","title":"Does Envist work with plain <code>.env</code> files?","text":"<p>Yes. If you omit type annotations, Envist behaves like a standard dotenv parser. Introduce types when you are ready.</p>"},{"location":"faq/#how-do-i-handle-secrets-securely","title":"How do I handle secrets securely?","text":"<p>Keep secrets out of version control. Store them in runtime environment variables or secret managers, then reference them with <code>${SECRET_NAME}</code> placeholders inside your typed <code>.env</code> file when necessary.</p>"},{"location":"faq/#what-happens-when-a-cast-fails","title":"What happens when a cast fails?","text":"<p>Envist raises <code>EnvistCastError</code> with details about the value and target type. Wrap <code>Envist()</code> creation in a <code>try</code> block or rely on <code>pytest</code> to catch misconfigurations during CI.</p>"},{"location":"faq/#can-i-override-values-at-runtime","title":"Can I override values at runtime?","text":"<p>Yes. Use <code>env.set(\"KEY\", value)</code> to update the in-memory map and <code>os.environ</code>. Call <code>env.save()</code> if you want to persist changes back to the file.</p>"},{"location":"faq/#how-do-i-toggle-features-per-environment","title":"How do I toggle features per environment?","text":"<p>Create one file per environment (e.g., <code>config/staging.env</code>, <code>config/production.env</code>) and load the right file using the <code>path</code> parameter. For additional overrides, merge dictionaries from multiple <code>Envist</code> instances.</p>"},{"location":"faq/#is-there-a-cli","title":"Is there a CLI?","text":"<p>Envist focuses on the Python API. Build lightweight tooling using <code>Envist</code> and libraries like <code>click</code> or <code>typer</code> to match your workflow.</p>"},{"location":"faq/#can-i-extend-the-caster-with-custom-types","title":"Can I extend the caster with custom types?","text":"<p>You can supply a callable to <code>env.get(key, cast=my_converter)</code>. For reusable annotations, wrap the callable and reference it from your application code.</p>"},{"location":"faq/#how-do-i-report-issues-or-request-features","title":"How do I report issues or request features?","text":"<p>Open an issue on GitHub. Include your <code>.env</code> snippet, stack trace, and environment details to help us reproduce the problem quickly.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Set up Envist in minutes and load typed configuration straight from your <code>.env</code> files. This guide walks through prerequisites, installation, and the core workflow.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.7 or newer</li> <li>An existing <code>.env</code> file (optional but recommended)</li> <li>Familiarity with standard environment variables</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#pip-recommended","title":"pip (recommended)","text":"<pre><code>pip install envist\n</code></pre>"},{"location":"getting-started/#conda","title":"Conda","text":"<pre><code>conda install -c conda-forge envist\n</code></pre>"},{"location":"getting-started/#poetry","title":"Poetry","text":"<pre><code>poetry add envist\n</code></pre>"},{"location":"getting-started/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/Almas-Ali/envist.git\ncd envist\npip install -e .\n</code></pre>"},{"location":"getting-started/#your-first-typed-env","title":"Your First Typed <code>.env</code>","text":"<p>Create a <code>.env</code> file alongside your application:</p> <pre><code>APP_NAME &lt;str&gt; = Envist Demo\nPORT &lt;int&gt; = 8080\nDEBUG &lt;bool&gt; = true\nDATABASE_URL &lt;str&gt; = postgresql://user:pass@localhost:5432/app\nALLOWED_HOSTS &lt;list&gt; = localhost, 127.0.0.1\n</code></pre> <p>Load it in Python:</p> <pre><code>from envist import Envist\n\nenv = Envist()\n\nprint(env.get(\"APP_NAME\"))       # 'Envist Demo'\nprint(env.get(\"PORT\"))           # 8080\nprint(env.get(\"ALLOWED_HOSTS\"))  # ['localhost', '127.0.0.1']\n</code></pre>"},{"location":"getting-started/#core-workflow","title":"Core Workflow","text":"<ol> <li>Annotate values in <code>.env</code> with <code>&lt;type&gt;</code> descriptors.</li> <li>Instantiate <code>Envist()</code> in your codebase.</li> <li>Retrieve values with <code>env.get()</code> or dictionary-style access (<code>env[\"KEY\"]</code>).</li> <li>Update configuration dynamically using <code>env.set()</code> and <code>env.save()</code> if required.</li> </ol>"},{"location":"getting-started/#switching-files","title":"Switching Files","text":"<p>Pass a custom path when you need environment-specific configs:</p> <pre><code>env = Envist(path=\"config/production.env\")\n</code></pre> <p>Combine configurations by instantiating multiple readers and merging dictionaries. See Multiple Environments for patterns.</p>"},{"location":"getting-started/#accepting-empty-values","title":"Accepting Empty Values","text":"<p>Some workflows require declaring keys without immediate values. Enable this by setting <code>accept_empty=True</code>:</p> <pre><code>env = Envist(accept_empty=True)\noptional = env.get(\"OPTIONAL_SETTING\")  # returns None when empty\n</code></pre>"},{"location":"getting-started/#disabling-auto-casting","title":"Disabling Auto Casting","text":"<p>To keep raw strings\u2014useful during incremental migrations\u2014set <code>auto_cast=False</code> and override per lookup:</p> <pre><code>env = Envist(auto_cast=False)\nsize = env.get(\"BATCH_SIZE\", cast=int)\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Learn about the declarative syntax in Smart .env Syntax.</li> <li>Explore complex data types in Type Casting &amp; Data Shapes.</li> <li>Add runtime safeguards with the Validation Recipes.</li> </ul>"},{"location":"guides/env-file-syntax/","title":"Smart .env Syntax","text":"<p>Envist extends the plain key=value format with annotations that tell the parser how to interpret and validate your configuration.</p>"},{"location":"guides/env-file-syntax/#anatomy-of-a-smart-line","title":"Anatomy of a Smart Line","text":"<pre><code>PORT &lt;int&gt; = 8080\n</code></pre> <ul> <li><code>PORT</code> is the key.</li> <li><code>&lt;int&gt;</code> is the type directive.</li> <li><code>8080</code> is the literal value.</li> </ul> <p>Type directives are optional\u2014omit them to keep backwards compatibility with classic <code>.env</code> files.</p>"},{"location":"guides/env-file-syntax/#supported-delimiters","title":"Supported Delimiters","text":"<ul> <li><code>KEY &lt;type&gt; = value</code> is the primary format.</li> <li><code>KEY:type=value</code> is also accepted for teams that prefer colon syntax.</li> <li><code>KEY=value</code> works exactly like traditional dotenv files.</li> </ul> <p>Whitespace around the equals sign is ignored. For readability, align values however you like.</p>"},{"location":"guides/env-file-syntax/#comments-and-empty-lines","title":"Comments and Empty Lines","text":"<ul> <li>Lines starting with <code>#</code> or containing <code>#</code> comments are ignored.</li> <li>Inline comments inside quotes are preserved: <code>SECRET &lt;str&gt; = \"value #secure\"</code>.</li> <li>Empty lines are skipped, making it easy to group related variables.</li> </ul>"},{"location":"guides/env-file-syntax/#declaring-empty-values","title":"Declaring Empty Values","text":"<p>When <code>Envist(accept_empty=True)</code> is enabled, you can leave values blank:</p> <pre><code># Placeholder for local overrides\nREDIS_URL &lt;str&gt; =\n</code></pre> <p>Without <code>accept_empty=True</code>, empty declarations are ignored to avoid surprises.</p>"},{"location":"guides/env-file-syntax/#environment-variable-names","title":"Environment Variable Names","text":"<p>Envist enforces conventional naming:</p> <ul> <li>Keys must start with a letter or underscore.</li> <li>Only alphanumeric characters and underscores are allowed.</li> </ul> <p>If a key fails validation, the loader raises <code>EnvistParseError</code>, helping you catch typos early.</p>"},{"location":"guides/env-file-syntax/#example-layout","title":"Example Layout","text":"<pre><code># Application metadata\nAPP_NAME &lt;str&gt; = Envist Demo\nAPP_VERSION &lt;str&gt; = 1.0.0\n\n# Server config\nHOST &lt;str&gt; = 0.0.0.0\nPORT &lt;int&gt; = 8000\nDEBUG &lt;bool&gt; = false\n\n# Database with variable expansion\nDB_HOST &lt;str&gt; = localhost\nDB_PORT &lt;int&gt; = 5432\nDATABASE_URL &lt;str&gt; = postgresql://user:pass@${DB_HOST}:${DB_PORT}/envist\n</code></pre>"},{"location":"guides/env-file-syntax/#migration-strategy","title":"Migration Strategy","text":"<ol> <li>Keep your existing <code>.env</code> file untouched.</li> <li>Introduce type annotations incrementally.</li> <li>Enable <code>auto_cast=True</code> (default) to benefit immediately.</li> </ol> <p>This approach keeps deployments safe while unlocking richer tooling over time.</p>"},{"location":"guides/environment-validation/","title":"Validation Recipes","text":"<p>The <code>@validator</code> decorator gives you a declarative way to assert that environment variables contain acceptable values before the rest of your application runs.</p>"},{"location":"guides/environment-validation/#quick-start","title":"Quick Start","text":"<pre><code>from envist import Envist, validator\n\nenv = Envist()\n\n@validator(env, \"PORT\")\ndef validate_port(value: int) -&gt; bool:\n    if not (1 &lt;= value &lt;= 65535):\n        raise ValueError(\"PORT must be within TCP range\")\n    return True\n</code></pre> <p>The decorator runs immediately, so misconfigured deployments fail fast.</p>"},{"location":"guides/environment-validation/#under-the-hood","title":"Under the Hood","text":"<ul> <li>Validators call <code>env.get(name)</code> using the same casting rules as the rest of your code.</li> <li>Envist checks keys with <code>EnvValidator.validate_key</code> before executing your custom logic, catching typos automatically.</li> <li>Any exception raised inside the validator stops startup\u2014use descriptive error messages for better DX.</li> </ul>"},{"location":"guides/environment-validation/#practical-examples","title":"Practical Examples","text":""},{"location":"guides/environment-validation/#guard-required-secrets","title":"Guard Required Secrets","text":"<pre><code>@validator(env, \"API_KEY\")\ndef validate_api_key(value: str) -&gt; bool:\n    if len(value) &lt; 32:\n        raise ValueError(\"API_KEY must be at least 32 characters long\")\n    return True\n</code></pre>"},{"location":"guides/environment-validation/#validate-choice-lists","title":"Validate Choice Lists","text":"<pre><code>VALID_LEVELS = {\"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\", \"CRITICAL\"}\n\n@validator(env, \"LOG_LEVEL\")\ndef validate_log_level(value: str) -&gt; bool:\n    if value.upper() not in VALID_LEVELS:\n        raise ValueError(f\"LOG_LEVEL must be one of {', '.join(sorted(VALID_LEVELS))}\")\n    return True\n</code></pre>"},{"location":"guides/environment-validation/#cross-field-validation","title":"Cross-Field Validation","text":"<p>Use existing getters to compare values:</p> <pre><code>@validator(env, \"MAX_CONNECTIONS\")\ndef validate_connection_limits(value: int) -&gt; bool:\n    min_connections = env.get(\"MIN_CONNECTIONS\", default=1)\n    if value &lt; min_connections:\n        raise ValueError(\"MAX_CONNECTIONS must be &gt;= MIN_CONNECTIONS\")\n    return True\n</code></pre>"},{"location":"guides/environment-validation/#best-practices","title":"Best Practices","text":"<ul> <li>Keep validators close to <code>Envist()</code> instantiation so they run during import.</li> <li>Raise <code>ValueError</code> with actionable messages instead of returning <code>False</code> silently.</li> <li>Group related validators into modules for complex systems (e.g., <code>validators/database.py</code>).</li> <li>Use standard Python typing to document expected input.</li> </ul>"},{"location":"guides/environment-validation/#testing-validators","title":"Testing Validators","text":"<p>Leverage fixtures to assert validator behavior:</p> <pre><code>import pytest\nfrom envist import Envist, validator\n\ndef test_port_validator(monkeypatch):\n    monkeypatch.setenv(\"PORT\", \"70000\")\n    env = Envist(auto_cast=True)\n    with pytest.raises(ValueError):\n        @validator(env, \"PORT\")\n        def validate_port(value: int) -&gt; bool:\n            if not (1 &lt;= value &lt;= 65535):\n                raise ValueError\n            return True\n</code></pre> <p>Envist reuses <code>os.environ</code> during parsing, so standard testing utilities like <code>monkeypatch</code> integrate seamlessly.</p>"},{"location":"guides/logging/","title":"Logging &amp; Observability","text":"<p>Envist ships with a dedicated logger so you can trace parsing, casting, and runtime mutations.</p>"},{"location":"guides/logging/#default-behavior","title":"Default Behavior","text":"<p>Without additional configuration, Envist:</p> <ul> <li>Logs warnings and errors to stdout.</li> <li>Writes a verbose log file to <code>~/.envist/envist.log</code>.</li> <li>Annotates events with function names and line numbers for quick debugging.</li> </ul>"},{"location":"guides/logging/#enabling-debug-mode","title":"Enabling Debug Mode","text":"<p>Set environment variables before instantiating <code>Envist</code>:</p> <pre><code>setx ENVIST_DEBUG true\nsetx ENVIST_LOG_LEVEL DEBUG\n</code></pre> <p>On Unix shells use <code>export</code> instead of <code>setx</code>. Debug mode increases console verbosity, making it easier to understand parsing decisions.</p>"},{"location":"guides/logging/#configuring-handlers-programmatically","title":"Configuring Handlers Programmatically","text":"<p>Use the factory helpers in <code>envist.logger</code>:</p> <pre><code>import logging\nfrom envist.logger import (\n    configure_logger,\n    create_stream_handler,\n    create_file_handler,\n)\n\nlogger = configure_logger(\n    handlers=[\n        create_stream_handler(level=logging.DEBUG),\n        create_file_handler(\"logs/envist.log\", level=logging.INFO),\n    ],\n    level=\"DEBUG\",\n)\n</code></pre> <p>Subsequent <code>Envist()</code> instances reuse the configured logger.</p>"},{"location":"guides/logging/#structured-logging","title":"Structured Logging","text":"<p>Write JSON payloads for ingestion into centralized platforms:</p> <pre><code>import logging\nfrom envist.logger import create_json_handler, EnvistLogger\n\nhandler = create_json_handler(\"logs/envist.json\", level=logging.INFO)\nEnvistLogger.configure(custom_handlers=[handler], reset=True)\n</code></pre> <p>Each record includes a timestamp, module, and message so observability tools can index them effectively.</p>"},{"location":"guides/logging/#rotating-logs","title":"Rotating Logs","text":"<p>Prevent disk bloat with rotating handlers:</p> <pre><code>import logging\nfrom envist.logger import create_rotating_handler, configure_logger\n\nconfigure_logger([\n    create_rotating_handler(\n        \"logs/envist.log\",\n        max_bytes=10 * 1024 * 1024,\n        backup_count=5,\n        level=logging.INFO,\n    )\n])\n</code></pre>"},{"location":"guides/logging/#runtime-signals","title":"Runtime Signals","text":"<p>The logger exposes helper methods for granular instrumentation:</p> <pre><code>from envist.logger import logger\n\nlogger.log_env_parse(\".env\", variables_found=12)\nlogger.log_typecast(\"PORT\", \"8080\", \"int\", success=True)\nlogger.log_variable_access(\"DATABASE_URL\", found=True, cast_type=\"str\")\n</code></pre>"},{"location":"guides/logging/#integrating-with-existing-loggers","title":"Integrating With Existing Loggers","text":"<p>Wrap Envist events into your application logger:</p> <pre><code>import logging\nfrom envist.logger import EnvistLogger\n\napp_logger = logging.getLogger(\"myapp\")\n\nclass ForwardHandler(logging.Handler):\n    def emit(self, record):\n        app_logger.log(record.levelno, \"[ENVIST] %s\", record.getMessage())\n\nEnvistLogger.configure(custom_handlers=[ForwardHandler()], reset=True)\n</code></pre>"},{"location":"guides/logging/#environment-variables-reference","title":"Environment Variables Reference","text":"Variable Description <code>ENVIST_DEBUG</code> When <code>true</code>, outputs debug logs to the console <code>ENVIST_LOG_LEVEL</code> Overrides the log level (<code>DEBUG</code>, <code>INFO</code>, etc.) <code>ENVIST_LOG_FORMAT</code> <code>standard</code> or <code>json</code> formatting <code>ENVIST_LOG_FILE</code> Absolute or relative path for file logging <code>ENVIST_ROTATING_LOGS</code> Enable rotation when combined with <code>ENVIST_LOG_FILE</code> <p>Fine-tuned logging turns configuration issues into actionable alerts rather than silent failures.</p>"},{"location":"guides/multi-environments/","title":"Multiple Environments","text":"<p>Envist adapts to multi-stage workflows\u2014development, staging, production\u2014without complex boilerplate.</p>"},{"location":"guides/multi-environments/#load-specific-files","title":"Load Specific Files","text":"<pre><code>from envist import Envist\n\nenv = Envist(path=\"config/production.env\")\n</code></pre> <p>Keep environment-specific files under <code>config/</code> and commit <code>.env.example</code> templates for visibility.</p>"},{"location":"guides/multi-environments/#layer-configurations","title":"Layer Configurations","text":"<p>Merge shared settings with overrides:</p> <pre><code>base = Envist(path=\"config/base.env\")\nlocal = Envist(path=\"config/local.env\")\n\nconfig = {**base.get_all(), **local.get_all()}\n</code></pre> <p>Prioritize the right order (<code>local</code> last) to emulate cascading configuration.</p>"},{"location":"guides/multi-environments/#feature-flags-toggles","title":"Feature Flags &amp; Toggles","text":"<p>Mix static values and dynamic flags:</p> <pre><code>FEATURE_FLAGS &lt;dict&lt;str, bool&gt;&gt; = auth:true, caching:false\n</code></pre> <p>Retrieve them as part of your runtime configuration dictionary.</p>"},{"location":"guides/multi-environments/#working-with-cicd","title":"Working With CI/CD","text":"<ul> <li>Store production secrets as environment variables in your deployment platform.</li> <li>Define fallbacks in <code>.env</code> for local development.</li> <li>Envist resolves <code>${VAR}</code> using both the file and <code>os.environ</code>, making the same code path work locally and in CI.</li> </ul>"},{"location":"guides/multi-environments/#handling-optional-values","title":"Handling Optional Values","text":"<p>Set <code>accept_empty=True</code> for staging environments where certain keys are intentionally blank:</p> <pre><code>staging_env = Envist(path=\"config/staging.env\", accept_empty=True)\n</code></pre> <p><code>None</code> values can signal disabled subsystems without extra conditionals.</p>"},{"location":"guides/multi-environments/#saving-changes","title":"Saving Changes","text":"<p>Use <code>.save()</code> when you need to persist runtime edits, such as CLI tools that manage environment files:</p> <pre><code>auto_env = Envist(path=\"config/generated.env\")\nauto_env.set(\"LAST_UPDATED\", \"2025-10-12\")\nauto_env.save(pretty=True, sort_keys=True)\n</code></pre> <p>Enable <code>example_file=True</code> to generate <code>.env.example</code> files that mirror your live environment without leaking secrets.</p>"},{"location":"guides/multi-environments/#recommended-directory-layout","title":"Recommended Directory Layout","text":"<pre><code>config/\n  base.env\n  development.env\n  staging.env\n  production.env\n</code></pre> <p>Pair this structure with Git-ignored <code>.env</code> secrets and version-controlled <code>.env.example</code> templates for consistency across teams.</p>"},{"location":"guides/type-casting/","title":"Type Casting &amp; Data Shapes","text":"<p>Envist ships with a powerful caster that understands primitive types, collections, and deeply nested annotations.</p>"},{"location":"guides/type-casting/#built-in-types","title":"Built-in Types","text":"Directive Python type Notes <code>&lt;str&gt;</code> <code>str</code> Preserves whitespace and casing <code>&lt;int&gt;</code> <code>int</code> Supports decimal numbers <code>&lt;float&gt;</code> <code>float</code> Parses scientific notation <code>&lt;bool&gt;</code> <code>bool</code> Accepts <code>true/false</code>, <code>1/0</code>, <code>yes/no</code>, <code>on/off</code> <code>&lt;list&gt;</code> / <code>&lt;array&gt;</code> <code>list[str]</code> Splits on commas, trims whitespace <code>&lt;tuple&gt;</code> <code>tuple[str, ...]</code> Same parsing as list, returns tuple <code>&lt;set&gt;</code> <code>set[str]</code> Removes duplicates automatically <code>&lt;dict&gt;</code> <code>dict[str, str]</code> Accepts <code>key:value</code> pairs or JSON <code>&lt;csv&gt;</code> <code>list[str]</code> Alias for comma-separated strings <code>&lt;json&gt;</code> <code>dict</code> / <code>list</code> Hands off to <code>json.loads</code>"},{"location":"guides/type-casting/#nested-collections","title":"Nested Collections","text":"<p>Use angle brackets to describe the shape of nested data:</p> <pre><code>PORTS &lt;list&lt;int&gt;&gt; = 8080, 8081\nFEATURE_FLAGS &lt;dict&lt;str, bool&gt;&gt; = auth:true, cache:false\nSERVERS &lt;tuple&lt;str&gt;&gt; = api.example.com, admin.example.com\n</code></pre> <p>Envist recursively casts each item, so <code>PORTS</code> becomes <code>[8080, 8081]</code> and <code>FEATURE_FLAGS</code> becomes <code>{\"auth\": True, \"cache\": False}</code>.</p>"},{"location":"guides/type-casting/#complex-structures","title":"Complex Structures","text":"<pre><code>CLUSTERS &lt;dict&lt;str, list&lt;int&gt;&gt;&gt; = east:8000,8001, west:9000,9001\nPIPELINES &lt;json&gt; = [\n  {\"name\": \"ingest\", \"enabled\": true},\n  {\"name\": \"analytics\", \"enabled\": false}\n]\n</code></pre> <p>The type caster understands JSON arrays, nested lists, and key=value syntax, falling back gracefully when formats mix.</p>"},{"location":"guides/type-casting/#manual-casting-at-runtime","title":"Manual Casting at Runtime","text":"<p>Disable auto casting globally or override per lookup:</p> <pre><code>env = Envist(auto_cast=False)\npage_size = env.get(\"PAGE_SIZE\", cast=int)\n</code></pre> <p>Runtime casts accept the same annotations (<code>\"list&lt;int&gt;\"</code>) or regular callables (<code>cast=list</code>).</p>"},{"location":"guides/type-casting/#casting-errors","title":"Casting Errors","text":"<ul> <li><code>EnvistCastError</code> is raised when a value cannot be converted.</li> <li>The message includes the original value and the target type to speed up debugging.</li> <li>When <code>accept_empty=True</code>, Envist tries to cast empty values to sensible defaults (e.g., an empty list for <code>&lt;list&lt;int&gt;&gt;</code>).</li> </ul>"},{"location":"guides/type-casting/#tips-for-reliable-data","title":"Tips for Reliable Data","text":"<ul> <li>Favor JSON for hierarchical payloads (<code>&lt;json&gt;</code>), especially when values include commas.</li> <li>Keep list items trimmed by adding a space after the comma: <code>item1, item2</code>.</li> <li>Use nested types for clarity instead of manual parsing in code.</li> <li>Pin boolean values to <code>true</code>/<code>false</code> for readability across tooling.</li> </ul>"},{"location":"guides/variable-expansion/","title":"Variable Expansion","text":"<p>Envist resolves <code>${OTHER_VAR}</code> placeholders so you can build configuration from reusable building blocks.</p>"},{"location":"guides/variable-expansion/#basic-expansion","title":"Basic Expansion","text":"<pre><code>HOST &lt;str&gt; = localhost\nPORT &lt;int&gt; = 8080\nBASE_URL &lt;str&gt; = http://${HOST}:${PORT}\n</code></pre> <p><code>env.get(\"BASE_URL\")</code> returns <code>\"http://localhost:8080\"</code>.</p>"},{"location":"guides/variable-expansion/#nested-references","title":"Nested References","text":"<p>Values can reference variables that in turn reference others. Envist walks the graph safely and detects loops:</p> <pre><code>API_HOST &lt;str&gt; = api.local\nAPI_BASE &lt;str&gt; = https://${API_HOST}\nHEALTH_ENDPOINT &lt;str&gt; = ${API_BASE}/health\n</code></pre> <p>All lookups resolve to fully expanded strings.</p>"},{"location":"guides/variable-expansion/#os-environment-fallback","title":"OS Environment Fallback","text":"<p>If a placeholder is not found in the <code>.env</code> file, Envist checks <code>os.environ</code> before substituting an empty string. This makes it easy to mix secrets injected by the host OS or CI server.</p>"},{"location":"guides/variable-expansion/#circular-reference-protection","title":"Circular Reference Protection","text":"<p>Envist keeps track of already-resolved keys and raises <code>EnvistParseError</code> when it detects a loop:</p> <pre><code>A &lt;str&gt; = ${B}\nB &lt;str&gt; = ${A}\n</code></pre> <p>Load-time feedback prevents runtime recursion failures.</p>"},{"location":"guides/variable-expansion/#collections-expansion","title":"Collections &amp; Expansion","text":"<p>Expansion happens before type casting, so nested values also work inside lists or dictionaries:</p> <pre><code>HOST_PRIMARY &lt;str&gt; = api.local\nHOST_SECONDARY &lt;str&gt; = admin.local\nHOSTS &lt;list&gt; = ${HOST_PRIMARY}, ${HOST_SECONDARY}\n</code></pre> <p>Expansion happens before casting, so <code>HOSTS</code> becomes <code>[\"api.local\", \"admin.local\"]</code>. Keep separate keys for values you want to reference individually.</p>"},{"location":"guides/variable-expansion/#troubleshooting-tips","title":"Troubleshooting Tips","text":"<ul> <li>Enable debug logging (<code>ENVIST_DEBUG=true</code>) to see expansion traces.</li> <li>Prefix optional values with defaults: <code>${OPTIONAL:-fallback}</code> is not supported yet; prefer dedicated keys for now.</li> <li>When mixing JSON structures and placeholders, ensure the expanded value remains valid JSON.</li> </ul>"},{"location":"reference/api/","title":"API Reference","text":"<p>This reference documents the public classes, methods, and decorators exposed by Envist.</p>"},{"location":"reference/api/#envist","title":"<code>Envist</code>","text":"<p>Main entry point for loading and managing environment variables.</p> <pre><code>from envist import Envist\n</code></pre>"},{"location":"reference/api/#constructor","title":"Constructor","text":"<pre><code>Envist(\n    path: str = \".env\",\n    accept_empty: bool = False,\n    auto_cast: bool = True,\n)\n</code></pre> Parameter Type Default Description <code>path</code> <code>str</code> <code>.env</code> File to load variables from <code>accept_empty</code> <code>bool</code> <code>False</code> Allow blank values and store them as <code>None</code> <code>auto_cast</code> <code>bool</code> <code>True</code> Automatically apply type annotations while parsing"},{"location":"reference/api/#properties","title":"Properties","text":"<ul> <li><code>path</code>: Returns the underlying file path.</li> </ul>"},{"location":"reference/api/#read-methods","title":"Read Methods","text":"Method Returns Notes <code>get(key, *, default=None, cast=None)</code> <code>Any</code> Retrieve a single value; optional runtime casting <code>get_all()</code> <code>dict[str, Any]</code> Copy of all parsed variables"},{"location":"reference/api/#write-methods","title":"Write Methods","text":"Method Returns Notes <code>set(key, value, cast=None)</code> <code>Any</code> Assigns a value and updates <code>os.environ</code> <code>set_all(mapping)</code> <code>None</code> Bulk assignment from a dictionary <code>unset(key)</code> <code>None</code> Delete a variable from Envist and <code>os.environ</code> <code>unset_all(keys=None)</code> <code>None</code> Remove listed keys or wipe everything <code>save(pretty=False, sort_keys=False, example_file=False)</code> <code>None</code> Persist values back to disk <code>reload()</code> <code>None</code> Re-read the file and refresh cached values"},{"location":"reference/api/#special-methods","title":"Special Methods","text":"<ul> <li><code>__getitem__</code>, <code>__setitem__</code>, <code>__delitem__</code>: Dictionary-style access (<code>env[\"KEY\"]</code>).</li> <li><code>__contains__</code>: Enables membership checks (<code>\"KEY\" in env</code>).</li> <li><code>__iter__</code> and <code>__len__</code>: Iterate over keys or inspect counts.</li> <li><code>__getattr__</code>: Attribute-style access when keys are valid identifiers (<code>env.PORT</code>).</li> </ul>"},{"location":"reference/api/#error-handling","title":"Error Handling","text":"<p>Most incorrect operations raise specific exceptions from <code>envist.core.exceptions</code>. See Exceptions for full details.</p>"},{"location":"reference/api/#validator","title":"<code>validator</code>","text":"<p>Decorator that validates configuration immediately after parsing.</p> <pre><code>from envist import validator\n\n@validator(env, \"KEY\")\ndef validate(value):\n    ...\n</code></pre> <ul> <li>Accepts an <code>Envist</code> instance and the variable name.</li> <li>Runs at definition time via an IIFE pattern.</li> <li>Raises when keys fail <code>EnvValidator.validate_key</code>.</li> </ul>"},{"location":"reference/api/#envistlogger","title":"<code>EnvistLogger</code>","text":"<p>Singleton wrapper around Python's logging module.</p> <pre><code>from envist.logger import EnvistLogger\n</code></pre>"},{"location":"reference/api/#key-methods","title":"Key Methods","text":"Method Description <code>configure(custom_handlers=None, reset=False)</code> Rebuild the singleton with new handlers <code>set_level(level)</code> Adjust log level at runtime <code>add_handler(handler)</code> / <code>remove_handler(handler)</code> Manage handlers dynamically <code>log_env_parse(path, variables_found)</code> Convenience logging helper <code>log_typecast(key, value, target_type, success)</code> Emit structured type casting messages <code>log_variable_access(key, found, cast_type=None)</code> Trace lookups <p>See Logging &amp; Observability for usage patterns.</p>"},{"location":"reference/api/#utilities","title":"Utilities","text":""},{"location":"reference/api/#create_stream_handlerstreamnone-levellogginginfo","title":"<code>create_stream_handler(stream=None, level=logging.INFO)</code>","text":"<p>Returns a ready-to-use <code>logging.StreamHandler</code> with Envist formatting.</p>"},{"location":"reference/api/#create_file_handlerlog_file-levellogginginfo","title":"<code>create_file_handler(log_file, level=logging.INFO)</code>","text":"<p>Creates a file handler that ensures the target directory exists.</p>"},{"location":"reference/api/#create_json_handlerlog_file-levellogginginfo","title":"<code>create_json_handler(log_file, level=logging.INFO)</code>","text":"<p>Writes logs as JSON records\u2014ideal for log aggregation systems.</p>"},{"location":"reference/api/#create_rotating_handlerlog_file-max_bytes10_485_760-backup_count5-levellogginginfo","title":"<code>create_rotating_handler(log_file, max_bytes=10_485_760, backup_count=5, level=logging.INFO)</code>","text":"<p>Generates a <code>RotatingFileHandler</code> for production setups.</p>"},{"location":"reference/api/#create_syslog_handleraddresslocalhost-514-levellogginginfo","title":"<code>create_syslog_handler(address=(\"localhost\", 514), level=logging.INFO)</code>","text":"<p>Sends Envist logs to Syslog.</p>"},{"location":"reference/api/#module-level-configuration","title":"Module-Level Configuration","text":"<p><code>envist.config.EnvistConfig</code> reads environment variables (<code>ENVIST_DEBUG</code>, <code>ENVIST_LOG_LEVEL</code>, etc.) and auto-configures logging on import. Instantiate it manually if you need to reapply settings after runtime changes.</p> <pre><code>from envist.config import EnvistConfig\n\nconfig = EnvistConfig()\nconfig.add_custom_handler(...)\n</code></pre>"},{"location":"reference/exceptions/","title":"Exceptions","text":"<p>Envist raises descriptive exceptions so you can distinguish parsing issues from type errors and missing keys.</p>"},{"location":"reference/exceptions/#hierarchy-overview","title":"Hierarchy Overview","text":"<pre><code>EnvistError (base)\n\u251c\u2500\u2500 EnvistParseError\n\u251c\u2500\u2500 EnvistCastError\n\u251c\u2500\u2500 EnvistTypeError\n\u251c\u2500\u2500 EnvistValueError\n\u2514\u2500\u2500 FileNotFoundError (Envist variant)\n</code></pre> <p>All exceptions live under <code>envist.core.exceptions</code> and are re-exported at the package root.</p>"},{"location":"reference/exceptions/#envisterror","title":"<code>EnvistError</code>","text":"<p>Base class for all custom exceptions. Catch this when you need a single fallback handler for any Envist-related failure.</p>"},{"location":"reference/exceptions/#envistparseerror","title":"<code>EnvistParseError</code>","text":"<p>Raised during file parsing when:</p> <ul> <li>Lines contain invalid syntax.</li> <li>Keys violate naming rules.</li> <li>Variable expansion hits circular references or missing placeholders.</li> </ul>"},{"location":"reference/exceptions/#envistcasterror","title":"<code>EnvistCastError</code>","text":"<p>Triggered when type casting fails, including nested conversions and runtime <code>env.get(..., cast=...)</code> calls. Error messages include the problematic value and target type.</p>"},{"location":"reference/exceptions/#envisttypeerror","title":"<code>EnvistTypeError</code>","text":"<p>Used by mutating methods (<code>set_all</code>, etc.) when passed incorrect data structures.</p>"},{"location":"reference/exceptions/#envistvalueerror","title":"<code>EnvistValueError</code>","text":"<p>Raised when attempting to <code>unset</code> or access keys that are absent from the current environment mapping.</p>"},{"location":"reference/exceptions/#filenotfounderror","title":"<code>FileNotFoundError</code>","text":"<p>Custom variant aligned with the standard library name. It is raised when the <code>.env</code> file cannot be located or read.</p> <p>Catch Envist's <code>FileNotFoundError</code> instead of the built-in one if you want to handle parser-specific messaging.</p>"},{"location":"reference/exceptions/#handling-exceptions-gracefully","title":"Handling Exceptions Gracefully","text":"<pre><code>from envist import Envist, EnvistError\n\ntry:\n    env = Envist(path=\"config/.env\")\nexcept EnvistError as exc:\n    print(f\"Configuration failed: {exc}\")\n</code></pre> <p>If you need precise control, catch specialized subclasses and tailor your remediation steps accordingly.</p>"}]}